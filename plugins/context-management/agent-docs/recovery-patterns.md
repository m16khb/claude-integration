# Recovery Patterns

> 세션 복구, 작업 연속성, 고급 활용 패턴

## Overview

Claude Code 세션 중단 시 작업 컨텍스트를 복구하고 개발 흐름의 연속성을 보장하는 패턴을 설명합니다.

```
RECOVERY WORKFLOW:
┌─────────────────────────────────────────────────────────┐
│                Session Interrupted                       │
│           (브라우저 종료, 네트워크 끊김 등)                  │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│              State Capture                               │
│    (Git status, Open files, Command history)             │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│         Context Reconstruction                           │
│    (작업 의도 추론, 다음 단계 추천)                         │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│              Resume Work                                 │
│         (중단 지점부터 재개)                               │
└─────────────────────────────────────────────────────────┘
```

---

## 복구 패턴

### 1. Git 기반 복구

```
GIT STATE RECOVERY:
├─ Unstaged 변경
│   ├─ 수정 중이던 파일 목록
│   ├─ 변경 내용 분석
│   └─ 의도 추론
│
├─ Staged 변경
│   ├─ 커밋 준비 중이던 파일
│   ├─ 커밋 메시지 초안
│   └─ 관련 파일 추가 검토
│
├─ Stash 항목
│   ├─ 임시 저장된 작업
│   ├─ Stash 메시지 분석
│   └─ 적용 가능 여부 확인
│
└─ 최근 커밋
    ├─ 작업 패턴 파악
    ├─ 다음 예상 작업
    └─ 관련 TODO 추출
```

### 2. 파일 기반 복구

```
FILE STATE RECOVERY:
├─ 최근 수정 파일
│   ├─ 수정 시간순 정렬
│   ├─ 편집 중이던 파일 우선
│   └─ 관련 파일 그룹화
│
├─ 미완료 코드 감지
│   ├─ TODO/FIXME 주석
│   ├─ console.log('TODO:')
│   ├─ throw new Error('Not implemented')
│   └─ 빈 함수 본문
│
├─ 의존성 추적
│   ├─ Import/Export 관계
│   ├─ 영향 받는 파일 목록
│   └─ 테스트 파일 매핑
│
└─ 컴파일 상태
    ├─ 타입 에러 위치
    ├─ 린트 에러 목록
    └─ 빌드 상태
```

### 3. 세션 히스토리 복구

```
SESSION HISTORY:
├─ 커맨드 히스토리
│   ├─ 실행한 슬래시 커맨드
│   ├─ 커맨드 파라미터
│   └─ 실행 순서 패턴
│
├─ 검색/탐색 기록
│   ├─ Grep 패턴
│   ├─ 열어본 파일 목록
│   └─ 탐색 경로
│
└─ 대화 컨텍스트
    ├─ 마지막 질문/요청
    ├─ 논의한 기능/버그
    └─ 결정 사항
```

---

## 복구 시나리오

### 시나리오 1: 기능 개발 중 중단

```
SCENARIO: 새 기능 개발 중 세션 종료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

복구 시작:
/context-management:continue-context

분석 결과:
┌─────────────────────────────────────────┐
│ 🔄 작업 복구: 결제 모듈 개발             │
│                                         │
│ 마지막 작업 (2시간 전):                  │
│ - payment.entity.ts 생성                │
│ - payment.service.ts 부분 구현           │
│ - payment.controller.ts 스텁             │
│                                         │
│ 미완료 항목:                             │
│ 1. PaymentService 메서드 구현            │
│    - processPayment() 본문 비어있음      │
│    - validatePayment() TODO 주석         │
│                                         │
│ 2. 테스트 파일 없음                      │
│    - payment.service.spec.ts 필요        │
│                                         │
│ 추천 다음 단계:                          │
│ 1. /context-management:inject-context   │
│    payment.service.ts                   │
│ 2. processPayment() 로직 구현            │
│ 3. validatePayment() 구현                │
│ 4. 테스트 작성                           │
└─────────────────────────────────────────┘
```

### 시나리오 2: 버그 수정 중 중단

```
SCENARIO: 긴급 버그 수정 중 세션 종료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

복구 시작:
/context-management:continue-context "인증 버그"

분석 결과:
┌─────────────────────────────────────────┐
│ 🔴 긴급 작업 복구: JWT 검증 버그          │
│                                         │
│ 브랜치: hotfix/jwt-validation            │
│                                         │
│ 변경 파일:                               │
│ M  auth/jwt.guard.ts                    │
│ M  auth/auth.service.ts                 │
│                                         │
│ Stash 항목:                              │
│ "WIP: 토큰 검증 로직 수정"                │
│                                         │
│ 추천 액션:                               │
│ 1. git stash pop                        │
│ 2. 변경사항 리뷰                         │
│ 3. 테스트 실행 (npm run test:e2e)       │
│ 4. 수정 완료 후 hotfix 머지               │
└─────────────────────────────────────────┘
```

### 시나리오 3: 리팩토링 중 중단

```
SCENARIO: 대규모 리팩토링 중 세션 종료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

복구 시작:
/context-management:continue-context

분석 결과:
┌─────────────────────────────────────────┐
│ ⚠️  복잡한 작업 복구: 서비스 레이어 재구성 │
│                                         │
│ 15개 파일 수정 중                        │
│ 7개 staged, 8개 unstaged                │
│                                         │
│ 리팩토링 패턴 감지:                      │
│ - UserService → UserDomainService       │
│ - 의존성 주입 구조 변경                  │
│                                         │
│ 위험 요소:                               │
│ - 타입 에러 3곳                          │
│ - 테스트 12개 실패                       │
│                                         │
│ 권장 전략:                               │
│ 1. 작은 단위로 커밋 분할                 │
│ 2. 각 커밋마다 테스트 통과 확인          │
│ 3. 단계별 리팩토링 재개:                 │
│    a. 타입 에러 해결                     │
│    b. 테스트 수정                        │
│    c. 남은 파일 리팩토링                 │
└─────────────────────────────────────────┘
```

---

## 고급 복구 패턴

### MCP Memory 활용

```yaml
# MCP Memory 연동 복구
recovery:
  mcp_memory:
    enabled: true
    # 이전 세션 메모리 로드
    load_previous: true
    memory_keys:
      - "current_task"
      - "decision_log"
      - "architecture_notes"

  # 복구 시 자동 로드
  auto_load:
    - 작업 관련 메모
    - 설계 결정 사항
    - 미완료 TODO 목록
```

사용 예시:
```bash
# 메모리와 함께 컨텍스트 복구
/context-management:continue-context --use-memory

# 특정 메모리 키 로드
/context-management:continue-context --memory-key "payment_module"
```

### Sequential Thinking 복구

```bash
# 단계적 사고로 작업 재구성
/context-management:continue-context --use-sequential-thinking

# 출력 예시:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🧠 Sequential Thinking: 작업 복구 분석

단계 1: 현재 상태 파악
- 브랜치: feature/notification
- 수정 파일: 8개
- 마지막 커밋: 3시간 전

단계 2: 작업 의도 추론
- 알림 시스템 구현 중
- WebSocket + FCM 연동
- 현재 진행도: 60%

단계 3: 중단 지점 식별
- notification.gateway.ts: WebSocket 핸들러 미완성
- fcm.service.ts: 토큰 관리 로직 TODO

단계 4: 다음 단계 추천
1. WebSocket 핸들러 완성
2. FCM 토큰 저장/조회 구현
3. 통합 테스트 작성
4. 에러 핸들링 추가

단계 5: 복구 계획
- 예상 소요 시간: 2-3시간
- 우선순위: HIGH (기능 개발 중)
- 리스크: 중간 (테스트 없음)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 작업 연속성 패턴

### 작업 체크포인트

```markdown
## 세션 종료 전 체크리스트

### 코드 상태
- [ ] 의미 있는 변경 단위로 커밋
- [ ] 미완료 코드에 TODO 주석
- [ ] Unstaged 변경 정리 (stash or commit)

### 문서화
- [ ] 다음 작업 계획 메모
- [ ] 설계 결정 사항 기록
- [ ] 어려운 부분 노트

### 환경 정리
- [ ] 테스트 통과 상태 확인
- [ ] 빌드 에러 없음
- [ ] Dependency 최신 상태
```

### 자동 체크포인트 설정

```yaml
# .claude/checkpoint-config.yml
checkpoint:
  enabled: true
  interval: 30m  # 30분마다 자동 체크포인트

  auto_save:
    - git_status
    - open_files
    - command_history
    - session_notes

  notification:
    enabled: true
    message: "💾 체크포인트 저장됨"
```

---

## 베스트 프랙티스

### DO ✅

```bash
# 세션 시작 시 항상 컨텍스트 복구
/context-management:continue-context

# 큰 작업은 작은 커밋으로 분할
git commit -m "feat(payment): entity 추가"
git commit -m "feat(payment): service 스켈레톤"

# 중단 전 현재 상태 메모
# TODO: 다음 작업 - validatePayment() 로직 구현
# NOTE: PortOne API 명세 확인 필요
```

### DON'T ❌

```bash
# 장시간 작업 후 한 번에 커밋 (복구 어려움)
# 15개 파일 수정 → 1개 커밋

# 미완료 코드에 주석 없이 방치
function processPayment() {
  // 빈 함수 본문
}

# Unstaged 변경사항 과다 누적
# 30개 파일 수정, 커밋 없음
```

---

## 통합 예시

### 일일 워크플로우

```bash
# 1. 아침: 어제 작업 복구
/context-management:continue-context

# 2. 작업 시작
/context-management:inject-context src/feature.ts

# 3. 중간 체크포인트 (점심 전)
git add -A
git commit -m "WIP: feature 구현 50%"

# 4. 오후 작업 재개
/context-management:continue-context

# 5. 작업 완료
/code-quality:review
/git-workflows:git-commit

# 6. 퇴근 전 정리
# TODO 주석 추가, 내일 계획 메모
```

### 팀 협업 시나리오

```bash
# A 개발자: 작업 전달
git add -A
git commit -m "WIP: 결제 모듈 70% - TODO: 테스트 작성"
git push origin feature/payment

# B 개발자: 작업 인수
git checkout feature/payment
/context-management:continue-context "결제 모듈"

# 출력:
# - 이전 작업자: A 개발자
# - 진행도: 70%
# - 미완료: 테스트 작성
# - 다음 단계: payment.service.spec.ts 작성
```

---

## 문제 해결

### 복구 실패 시

```bash
# 1. Git 상태 확인
git status
git log --oneline -10

# 2. Stash 확인
git stash list
git stash show -p stash@{0}

# 3. 변경사항 백업
git diff > backup.patch
git diff --staged > staged.patch

# 4. 클린 상태로 복구
git stash
git reset --hard HEAD

# 5. 필요 시 백업 적용
git apply backup.patch
```

### 충돌 해결 후 복구

```bash
# Merge 충돌 해결 후
git add -A
git merge --continue

# 컨텍스트 재분석
/context-management:continue-context
```

---

@../CLAUDE.md | @chunking-algorithm.md | @context-analysis.md
