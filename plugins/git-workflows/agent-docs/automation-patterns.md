# Automation Patterns

> 다중 커밋, 스마트 그룹화, 자동화 기능

## Overview

변경사항을 지능적으로 분석하고 논리적 단위로 자동 커밋하는 패턴을 설명합니다.

```
AUTOMATION PIPELINE:
┌─────────────────────────────────────────────────────────┐
│                  Git Changes                             │
│           (staged + unstaged files)                      │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│               Change Analysis                            │
│  ├─ File type detection                                 │
│  ├─ Path-based grouping                                 │
│  └─ Change pattern recognition                          │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│              Group Formation                             │
│  ├─ Semantic grouping (auth, user, ...)                │
│  ├─ Type-based grouping (test, docs, ...)              │
│  └─ Commit type suggestion                              │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│            Commit Strategy Decision                      │
│  ├─ Single commit (1 group)                            │
│  ├─ Multi commit (2+ groups)                           │
│  └─ Interactive selection                               │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│               Commit Execution                           │
│  ├─ Message generation                                  │
│  ├─ Staging & commit                                    │
│  └─ Optional push                                       │
└─────────────────────────────────────────────────────────┘
```

---

## 변경사항 그룹화

### 그룹화 알고리즘

```
GROUPING ALGORITHM:
1. Primary Grouping (경로 기반)
   ┌─────────────────────────────────────────┐
   │ src/auth/*        → "auth" group        │
   │ src/user/*        → "user" group        │
   │ src/payment/*     → "payment" group     │
   │ src/common/*      → "common" group      │
   │ tests/*           → "test" group        │
   │ docs/*            → "docs" group        │
   │ config/*          → "config" group      │
   └─────────────────────────────────────────┘

2. Secondary Grouping (파일 타입)
   ┌─────────────────────────────────────────┐
   │ *.test.ts, *.spec.ts  → "test" group    │
   │ *.md                   → "docs" group    │
   │ package.json, *.yml    → "config" group │
   │ *.ts, *.js             → "source" group │
   └─────────────────────────────────────────┘

3. Tertiary Grouping (변경 패턴)
   ┌─────────────────────────────────────────┐
   │ A (added)     → feat (likely)           │
   │ M (modified)  → fix/refactor (likely)   │
   │ D (deleted)   → refactor/chore (likely) │
   │ R (renamed)   → refactor (likely)       │
   └─────────────────────────────────────────┘
```

### 그룹화 예시

```
EXAMPLE: 8 files changed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Input Files:
  M  src/auth/auth.service.ts
  M  src/auth/jwt.guard.ts
  A  src/auth/dto/login.dto.ts
  A  tests/auth/auth.service.spec.ts
  A  tests/auth/jwt.guard.spec.ts
  M  docs/api/auth.md
  M  package.json
  M  README.md

Grouping Result:
┌─ Group 1: auth (source) ──────────────────────┐
│ Type: feat                                     │
│ Files:                                         │
│   M  src/auth/auth.service.ts                  │
│   M  src/auth/jwt.guard.ts                     │
│   A  src/auth/dto/login.dto.ts                 │
│ Summary: JWT 인증 시스템 구현                   │
└────────────────────────────────────────────────┘

┌─ Group 2: test ───────────────────────────────┐
│ Type: test                                     │
│ Files:                                         │
│   A  tests/auth/auth.service.spec.ts           │
│   A  tests/auth/jwt.guard.spec.ts              │
│ Summary: 인증 모듈 테스트 추가                  │
└────────────────────────────────────────────────┘

┌─ Group 3: docs ───────────────────────────────┐
│ Type: docs                                     │
│ Files:                                         │
│   M  docs/api/auth.md                          │
│   M  README.md                                 │
│ Summary: 인증 API 문서 업데이트                 │
└────────────────────────────────────────────────┘

┌─ Group 4: config ─────────────────────────────┐
│ Type: chore                                    │
│ Files:                                         │
│   M  package.json                              │
│ Summary: 의존성 업데이트                        │
└────────────────────────────────────────────────┘
```

---

## 다중 커밋 전략

### 결정 트리

```
DECISION TREE:
┌─────────────────────────────────────────────┐
│ IF groups.length == 1                       │
│   → SINGLE COMMIT MODE                      │
│   → 기존 방식으로 단일 커밋                   │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ IF groups.length >= 2 AND <= 5              │
│   → MULTI COMMIT MODE                       │
│   → TUI로 사용자 선택 제공                   │
│   → 옵션:                                    │
│     [1] 그룹별 개별 커밋 (권장)              │
│     [2] 전체 단일 커밋                       │
│     [3] 선택적 커밋                          │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ IF groups.length > 5                        │
│   → WARN: "변경이 너무 많습니다"             │
│   → SUGGEST: git stash로 일부 보류           │
│   → 또는 --batch 모드로 자동 분할            │
└─────────────────────────────────────────────┘
```

### TUI 선택 화면

```
커밋 전략 선택
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4개 그룹으로 변경사항이 분류되었습니다.

[1] 그룹별 개별 커밋 ⭐ (Recommended)
    각 그룹을 별도 커밋으로 생성합니다.
    히스토리가 깔끔하고 리뷰하기 쉽습니다.

    예상 커밋 수: 4개
    - feat(auth): JWT 인증 구현
    - test(auth): 테스트 추가
    - docs: API 문서 업데이트
    - chore(deps): 의존성 업데이트

[2] 전체 단일 커밋
    모든 변경을 하나의 커밋으로 묶습니다.

    예상 커밋: 1개
    - feat(auth): 인증 시스템 구현 및 문서화

[3] 선택적 커밋
    커밋할 그룹을 직접 선택합니다.

    그룹 선택 화면으로 이동 →

[q] 취소
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
선택 (1-3, q):
```

### 그룹별 커밋 실행

```
GROUP COMMIT EXECUTION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[1/4] auth 그룹 커밋 중...
✅ feat(auth): JWT 기반 인증 시스템 구현
   3 files changed, 245 insertions(+), 12 deletions(-)
   abc1234

[2/4] test 그룹 커밋 중...
✅ test(auth): 인증 모듈 테스트 추가
   2 files changed, 180 insertions(+)
   def5678

[3/4] docs 그룹 커밋 중...
✅ docs: 인증 API 문서 업데이트
   2 files changed, 45 insertions(+), 8 deletions(-)
   ghi9012

[4/4] config 그룹 커밋 중...
✅ chore(deps): jsonwebtoken 패키지 추가
   1 file changed, 3 insertions(+), 1 deletion(-)
   jkl3456

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ 4개 커밋 완료

푸시하시겠습니까? (y/n):
```

---

## 커밋 순서 최적화

### 권장 순서

```
RECOMMENDED COMMIT ORDER:
1. chore/config  → 설정 변경 (의존성 추가)
   이유: 다른 코드가 이 설정에 의존할 수 있음

2. refactor     → 구조 변경
   이유: 새 기능이 리팩토링된 구조를 사용

3. feat         → 새 기능
   이유: 기능 구현이 핵심

4. fix          → 버그 수정
   이유: 기능 완성 후 발견된 문제 해결

5. test         → 테스트 추가
   이유: 구현 완료 후 테스트 작성

6. docs         → 문서화
   이유: 모든 작업 완료 후 문서 정리
```

### 순서 조정 예시

```
INPUT (변경 순서):
1. docs → 2. feat → 3. test → 4. config

REORDERED (최적 순서):
1. config → 2. feat → 3. test → 4. docs

REASON:
config: package.json에 jwt 패키지 추가
  └─> feat: jwt를 사용하는 auth 구현
       └─> test: auth 테스트 작성
            └─> docs: API 문서 업데이트
```

---

## 스마트 스테이징

### 자동 스테이징 규칙

```yaml
smart_staging:
  # 자동 스테이징 대상
  auto_stage:
    - "src/**/*.{ts,js,py,go}"
    - "tests/**/*.{test,spec}.{ts,js}"
    - "docs/**/*.md"

  # 수동 확인 필요
  require_confirmation:
    - "package.json"
    - "*.config.*"
    - ".env*"
    - "*.yml"

  # 절대 스테이징 금지
  never_stage:
    - ".env"
    - "*.pem"
    - "*.key"
    - "*secret*"
    - "*credential*"
    - "node_modules/**"
```

### 크기 기반 처리

```
SIZE-BASED HANDLING:
┌──────────────────┬──────────────────────────────────┐
│ 파일 크기         │ 처리 방식                         │
├──────────────────┼──────────────────────────────────┤
│ < 1KB            │ 자동 스테이징                     │
│ 1KB ~ 10KB       │ 자동 스테이징 (일반적)            │
│ 10KB ~ 100KB     │ 경고 + 확인 필요                  │
│ > 100KB          │ 거부 + 분할 권장                  │
│ > 1MB            │ Git LFS 권장                      │
└──────────────────┴──────────────────────────────────┘
```

---

## 그룹 병합 규칙

### 병합 조건

```
MERGE CONDITIONS:
├─ 같은 모듈 + 같은 타입
│   예: src/auth/*.ts (모두 기능 추가)
│   결과: 하나의 feat(auth) 커밋으로 병합
│
├─ 소스 + 테스트 (같은 모듈)
│   예: src/auth/service.ts + tests/auth/service.spec.ts
│   결과: feat(auth) 하나로 병합 (선택적)
│
├─ 문서 파일들
│   예: README.md, docs/api.md, CHANGELOG.md
│   결과: docs 하나로 병합
│
└─ 설정 파일들
    예: package.json, tsconfig.json, .eslintrc
    결과: chore 하나로 병합
```

### 분리 조건

```
SPLIT CONDITIONS:
├─ 서로 다른 모듈
│   예: src/auth/*.ts + src/payment/*.ts
│   결과: auth와 payment 각각 커밋
│
├─ 서로 다른 타입
│   예: 새 기능 + 버그 수정
│   결과: feat와 fix 각각 커밋
│
├─ 우선순위 차이
│   예: 긴급 수정 + 일반 기능
│   결과: hotfix 먼저, feature 나중에
│
└─ 논리적 독립성
    예: 인증 구현 + 결제 구현
    결과: 각각 독립적으로 커밋
```

---

## 보안 자동 검사

### 민감 파일 감지

```
SECURITY CHECKS:
┌──────────────────────────────────────────────┐
│ 민감 파일 패턴                                │
├──────────────────────────────────────────────┤
│ .env, .env.*                                 │
│ *secret*, *credential*, *password*           │
│ *.pem, *.key, *.p12, *.pfx                   │
│ id_rsa, id_dsa, *.ppk                        │
│ *token*, *apikey*                            │
│ firebase-adminsdk-*.json                     │
│ service-account*.json                        │
└──────────────────────────────────────────────┘

IF detected:
  1. 즉시 경고 표시
  2. 스테이징 자동 취소
  3. .gitignore 추가 제안
  4. 사용자 확인 요청
```

### main/master 브랜치 보호

```
MAIN BRANCH PROTECTION:
IF current_branch in ['main', 'master']:
  IF 'push' in arguments:
    ⚠️  WARNING: main 브랜치에 직접 푸시 시도

    ❓ 정말 main에 직접 푸시하시겠습니까?
       이 작업은 매우 위험합니다.

    권장 워크플로우:
    1. feature 브랜치 생성
    2. 개발 및 커밋
    3. PR 생성 및 리뷰
    4. develop/main 머지

    계속하시겠습니까? (yes/no):

  ELSE:
    ℹ️  INFO: main 브랜치에 커밋 중입니다.
        푸시는 하지 않습니다.
```

---

## 이슈 트래커 통합

### 자동 이슈 연결

```yaml
issue_tracker:
  enabled: true
  type: "jira"  # jira, github, gitlab, linear

  # 이슈 감지 패턴
  patterns:
    - "PROJ-\\d+"       # JIRA
    - "#\\d+"           # GitHub
    - "!\\d+"           # GitLab MR

  # 브랜치에서 이슈 추출
  extract_from_branch: true
  # 예: feature/PROJ-123-login → Closes PROJ-123

  # 커밋 메시지에 자동 추가
  auto_link: true
  format: "Closes {issue}"

  # PR 생성 시 이슈 자동 연결
  link_to_pr: true
```

### 이슈 연결 예시

```
EXAMPLE 1: 브랜치에서 추출
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
브랜치: feature/PROJ-123-user-login

생성된 커밋:
feat(auth): 사용자 로그인 기능 구현

Closes PROJ-123

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EXAMPLE 2: 커밋 메시지에서 추출
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
사용자 입력:
/git-commit --issue PROJ-456

생성된 커밋:
fix(api): N+1 쿼리 문제 해결

Closes PROJ-456

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 코드 리뷰 연동

### Pre-commit 훅

```bash
#!/bin/sh
# .git/hooks/pre-commit
# 자동 실행: 커밋 직전

echo "🔍 코드 검증 중..."

# 1. Linter 실행
npm run lint
if [ $? -ne 0 ]; then
  echo "❌ Lint 실패"
  exit 1
fi

# 2. 타입 체크
npm run type-check
if [ $? -ne 0 ]; then
  echo "❌ 타입 체크 실패"
  exit 1
fi

# 3. 테스트 실행
npm run test
if [ $? -ne 0 ]; then
  echo "❌ 테스트 실패"
  exit 1
fi

echo "✅ 모든 검증 통과"
exit 0
```

### 훅 실패 처리

```
HOOK FAILURE HANDLING:
┌─────────────────────────────────────────────┐
│ Pre-commit hook failed                      │
├─────────────────────────────────────────────┤
│ ❌ Lint 실패: 3개 오류 발견                  │
│                                             │
│ src/auth/service.ts:45                      │
│   Unexpected console.log statement          │
│                                             │
│ src/user/controller.ts:23                   │
│   Missing return type                       │
│                                             │
│ src/payment/dto.ts:12                       │
│   Unused variable 'amount'                  │
├─────────────────────────────────────────────┤
│ 옵션:                                        │
│ [1] 오류 수정 후 다시 커밋                   │
│ [2] 훅 건너뛰기 (권장하지 않음)              │
│ [q] 취소                                     │
└─────────────────────────────────────────────┘
```

---

## 고급 자동화

### Batch 모드

```bash
# 대량 변경 자동 분할 커밋
/git-workflows:git-commit --batch

# 프로세스:
1. 변경사항 스캔 (50+ files)
2. 자동 그룹화 (10 groups)
3. 논리적 순서로 재정렬
4. 각 그룹별 자동 커밋
5. 결과 리포트 출력

# 출력:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 Batch Commit 완료

총 10개 그룹 → 10개 커밋 생성
- chore(deps): 의존성 업데이트
- refactor(core): 공통 모듈 구조 개선
- feat(auth): 인증 시스템 구현
- feat(user): 사용자 관리 기능
- feat(payment): 결제 모듈 추가
- test(auth): 인증 테스트
- test(user): 사용자 테스트
- test(payment): 결제 테스트
- docs: API 문서 업데이트
- docs: README 업데이트

총 변경: +2,458 / -345 in 52 files
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### CI/CD 통합

```yaml
# .github/workflows/commit-check.yml
name: Commit Message Check

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Validate commit messages
        uses: wagoid/commitlint-github-action@v5
        with:
          configFile: .commitlintrc.json

      - name: Check commit format
        run: |
          git log --format=%s -1 | \
          grep -E "^(feat|fix|docs|style|refactor|test|chore|perf|ci)(\(.+\))?: .+"
```

---

## Best Practices

### DO ✅

```bash
# 논리적 단위로 커밋 분할
/git-workflows:git-commit
# → 자동 그룹화 활용

# 민감 파일 제외
# → 자동 감지 및 경고

# 이슈 연결
/git-workflows:git-commit --issue PROJ-123
# → 이슈 자동 연결

# 커밋 전 검증
# → Pre-commit 훅 활용
```

### DON'T ❌

```bash
# 모든 변경을 하나로 커밋
# → 그룹화 무시

# 민감 파일 커밋
# → .env, *.key 등

# 이슈 번호 누락
# → 추적 어려움

# 훅 무시
git commit --no-verify
# → 품질 저하
```

---

@../CLAUDE.md | @commit-conventions.md | @branch-strategies.md
